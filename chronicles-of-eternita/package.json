{
  "name": "chronicles-of-eternita",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install && node scripts/post-setup.js",
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend"
  }
}
